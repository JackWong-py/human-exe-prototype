# Direct dependencies only. pip installs everything else automatically.
fastapi==0.141.1
uvicorn==0.53.0
python-dotenv==1.2.3
google-genai==2.24.0
httpx==0.28.1        # used by FastAPI's TestClient in the tests

# Needed by B (txt/xlsx/docx readers) and C (PDF and scan readers):
openpyxl
python-docx
pdfplumber
pytesseract
pillow
